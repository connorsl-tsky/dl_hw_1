# dl_hw_1
linear regression and some deep neural networks. deep learning hw 1. uploaded to kaggle eventually

in git_bash run `code . &`

to upload it to kaggle, zip up the necessary files (e.g. classes and functions), upload it as a dataset, in the notebook do something like `sys.path.append('/kaggle/input/datasets/<username>/<datasetname>/<zipfilename>/')` and `from pythonmodeulfile import function, Class`

thank you gemini for the quick questions, those don't count towards the assignment
i just don't know how to use kaggle
